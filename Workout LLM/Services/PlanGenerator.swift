import Foundation
import SwiftData

// PlanGenerator is no longer needed.
// The plan is now computed at render time from StaticPlan.
// User overrides (add/remove) are stored as PlanOverride rows.
// This file is kept empty to avoid Xcode build errors from missing references.
